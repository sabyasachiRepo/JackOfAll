<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.tools.news">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_news">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <provider
            android:name="com.tools.news.NewsProvider"
            android:authorities="${applicationId}.news.initializationProvider"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>